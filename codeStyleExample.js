// if, else if, else statements
if(...) [one short line of code]
else if(...) [one short line of code]
else [one short line of code]

if(...) {
  [multiple lines of code, or a single long line of code]
} 
else if(...) {
  [multiple lines of code, or a single long line of code]
} 
else {
  [multiple lines of code, or a single long line of code]
}

// For loops
for(let i = 0; i < LENGTH; ++i) {
  [code]
}

// Nested for loops
for(let i = 0; i < LENGTH; ++i) {
  for(let j = 0; i < LENGTH; ++j)
}

